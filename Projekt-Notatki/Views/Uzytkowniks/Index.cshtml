@model IEnumerable<Projekt_Notatki.Models.uzytkownik>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nazwa_uzytkownika)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.haslo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nazwa_uzytkownika)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.haslo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_uzytkownik">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_uzytkownik">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_uzytkownik">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
